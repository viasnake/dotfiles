#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "[WARNING] script/opencode/run-with-bw is deprecated; use script/opencode/run-with-secrets" >&2
exec "$SCRIPT_DIR/run-with-secrets" "$@"
